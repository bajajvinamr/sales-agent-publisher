# Retrospection — 2026-04-23

## Velocity
20 commits in last 14 days. CI green locally via `act` (GitHub Actions blocked at account level, workaround in place). 3 tests passing / 3 total (new — vitest bootstrapped this retro). Build: green.

## What we changed

| Commit | Concern |
|---|---|
| `0091fe6` | `docs: add project CLAUDE.md with behavioral rules` — 57 lines codifying non-obvious invariants |
| `7ef8559` | `test: add vitest + 3 seed tests on critical pure logic` — JID filter, dedup hash, excel DB→xlsx mapping |
| `cb04c3f` | `ci: add gitleaks + fix ESLint v9 flat config` — secret scan + flat config shim + CI step fix |
| `702ffc6` | `docs: add CONTINUE.md session handoff` — includes dependabot triage |
| (pending) | `docs: retrospection 2026-04-23` — this file |

Also updated out-of-tree: global memory `reference_salestracker_droplet.md` — removed stale Tailscale refs, replaced with Cloudflare quick-tunnel + `get-url.sh` helper.

## What we observed but didn't fix
- **ESLint step still `continue-on-error: true`.** Config now valid but the repo isn't clean enough to gate on. Ratchet later — flip to `false` after a scheduled cleanup pass.
- **No Playwright E2E against preview URL.** Listed in CI gate audit but out of scope for this retro. HEALTH.md flags it as a known limit.
- **Dependabot queue (7 PRs).** Triaged into low/medium/defer in CONTINUE.md. Not merged here because each needs its own `act` run.
- **8097 LOC src/, no splits.** `whatsapp-baileys.ts` at 672 lines and `orchestrator.ts` at 510 are approaching the 800-line refactor threshold. Watch, don't act.
- **No ADRs.** 90+ days of active development without a single architecture decision record. Major decisions (Baileys over whatsapp-web.js; Cloudflare quick-tunnel over Tailscale Funnel; local `act` over fighting the GH Actions block) are implicit in commit messages only. Add `docs/adr/` when next major decision lands.

## Delta to workflow

| Metric | Before | After |
|---|---|---|
| Project `CLAUDE.md` | missing | 57 lines |
| `CONTINUE.md` | missing | present |
| Unit tests | 0 | 3 (seed) |
| CI secret scan | none | gitleaks full-history |
| ESLint config | broken (v10 + no flat config) | v9 + next preset flat config |
| CI steps in `check` job | 5 | 6 (+ vitest) |
| Memory files with stale refs | ≥1 | 0 (verified) |
| Local CI runner | new this session | cached, ~2 min runs |

## Observations for next retro
- **Dependabot cadence:** queue will grow weekly. Without a merge rhythm, every retro will have "N open dependabot PRs" as a 🟡 drift. Consider adding a monthly "dependabot sweep" to the calendar or a semi-auto merger for the patch group.
- **Test coverage delta:** went from 0 → 3. Next retro should show growth tied to touched modules — the rule should be "every bug fix lands with a regression test." Watch for commits that fix behavior but don't add a test.
- **`act` cache longevity:** `setup-node@v4` cache inside act is keyed on `package-lock.json` hash. Any dependabot merge invalidates it, back to 18-min runs. Worth testing whether the cache survives across dependabot merges or not.
- **The GH Actions outage:** if it resolves, verify that local `act` didn't drift from real runner behavior. Until then, `act` is the canonical gate.
- **CLAUDE.md rot risk:** the file currently describes this repo accurately. It will silently drift when someone changes the docker-compose boot command, the JID filter, or the timezone. No automated check enforces it. Future drift will be caught by humans or by Claude failing weirdly.

## Next retro target
2026-05-21 (~4 weeks). Earlier if: (a) GH Actions unblocks and `act`-vs-real drift needs audit, (b) a dependabot PR breaks prod, or (c) test count hasn't grown beyond 3.
